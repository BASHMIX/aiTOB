

Please implement the following updates exactly as described:

### 1. Active Match Status Component (UI & Actions)
- **Add a Close (×) Button:** Add a small '×' icon button at the top-right corner of the Active Match card. When clicked, it should remove the match from the "Active Match Status" section and return it to the "Match Dashboard" lists (e.g., back to 'In Progress' or 'Not Started').
- **Revert Styling:** The current colors and design of the Active Match cards are visually clashing and do not fit the overall dashboard theme. Revert to the older, cleaner design. Ensure the background colors, borders, and text colors perfectly match the global dark/navy theme of the rest of the dashboard.

### 2. Match Dashboard Component (Sorting & Filtering)
Add a new "Controls/Filters Row" directly below the "Match Dashboard" title.
- **TBD Sorting Logic (Crucial):** Across ALL sections (Not Started, Players Called, etc.), modify the sorting logic. Matches where BOTH players are known (P1 & P2 are assigned) MUST always appear at the top of the list. Matches containing "TBD" (To Be Determined) for one or both players must be pushed to the bottom.
- **Hide TBD Checkbox:** Add a checkbox labeled "Hide TBD Matches" in the controls row. When checked, any match card containing a "TBD" player should be hidden from the view entirely.
- **PhaseGroup (Pool) Filter:** If the tournament has multiple `phaseGroup(id)`s (e.g., Pool A1, Pool B2), add a dropdown menu or a horizontal list of checkboxes in the controls row. 
  - It must include a "Select All / Show All" default option.
  - Allowing the Admin to filter the dashboard to only show matches from specific pools.

### 3. Match Card Component (Data Display)
- **Display PhaseGroup Name:** On every individual match card (across all sections), display the name of the `phaseGroup` (e.g., "Pool A1" or "Winners Bracket") so the Admin knows exactly which pool the match belongs to. Place this neatly near the Match ID or Round Name.

### 4. Complete Matches Section (Action Buttons)
- **Remove Redundant Button:** Currently, completed match cards have both a "Reopen" and a "Reset" button which perform the exact same function (calling `markSetAsIncomplete` mutation). **Remove the "Reopen" button entirely.** Keep only the "Reset" button to avoid confusion.

**Deliverables required from you:**
1. Provide the updated React component code for the `MatchCard` (including the X button and PhaseGroup name).
2. Provide the sorting and filtering functions (TypeScript/JavaScript logic) that handle the TBD top/bottom sorting, the "Hide TBD" state, and the PhaseGroup filtering.
3. Provide the updated CSS/Tailwind classes to ensure the Active Match card matches the global dashboard theme.